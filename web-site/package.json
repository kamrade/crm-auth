{
  "name": "node-express-mongo",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {

    "serve": "nodemon --exec babel-node -- ./server.js",
    "start:dev": "export NODE_ENV=dev || SET NODE_ENV=dev && npm run serve",
    "start": "nodemon --exec babel-node -- ./server.js",

    "test:once": "export NODE_ENV=test || SET NODE_ENV=test && mocha server.test.js",
    "test": "nodemon --exec 'npm run test:once'"

  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.7.0",
    "babel-preset-es2015": "^6.24.1",
    "body-parser": "^1.18.3",
    "cors": "^2.8.4",
    "express": "^4.16.3",
    "mongoose": "^5.2.6",
    "nodemon": "^1.18.3"
  }
}
